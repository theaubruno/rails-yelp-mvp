  <%= simple_form_for @restaurant do |f| %>
      <%= f.input :name, hint: 'You cannot change your username.' %>
      <%= f.input :address %>
      <%= f.input :phone_number %>
      <%= f.input :category, collection: Restaurant::CATEGORIES, as: :radio_buttons%>

    <%= f.button :submit %>
  <% end %>
