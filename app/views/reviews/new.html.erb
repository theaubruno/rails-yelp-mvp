  <%= simple_form_for [@restaurant, @review] do |f| %>
      <%= f.input :content %>
      <%= f.input :rating, collection: Review::RATING, as: :radio_buttons %>

    <%= f.button :submit %>
  <% end %>
